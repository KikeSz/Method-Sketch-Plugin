{
  "name": "method",
  "description": "",
  "repository": "https://github.com/KikeSz/Method-Sketch-Plugin",
  "version": "1.2.5",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "Method",
    "manifest": "src/manifest.json",
    "main": "method.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.11"
  }
}
